import SwiftUI

extension Color {
    static let priceDropGreen = Color(red: 0.20, green: 0.72, blue: 0.42)
    static let priceIncreaseRed = Color(red: 0.92, green: 0.28, blue: 0.28)
    static let cardBackground = Color(uiColor: .secondarySystemGroupedBackground)
    static let screenBackground = Color(uiColor: .systemGroupedBackground)
}
